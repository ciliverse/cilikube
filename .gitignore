node_modules
web/node_modules
web/dist
desktop/node_modules
desktop/release
desktop/resources-staging
desktop/dist
.idea/
minikube-darwin-arm64
output/
# Ignore build artifacts
dist/
build/
out/
.nuxt/
.next/
.output/
.cache/
.parcel-cache/
.vite/
.rollup.cache/
# Ignore IDE specific files
.idea/
# Ignore Minikube binary for macOS ARM64
minikube-darwin-arm64
# Ignore output directory
output/
# Ignore Cilikube binary and related files (root only; do not ignore deployments/helm/cilikube)
/cilikube
/cilikube.exe
/cilikube-*
# Compiled Go server binary (曾被误提交，导致历史膨胀)
cmd/server/server
# Runtime logs (曾被误提交，导致历史膨胀)
*.log
# Runtime SQLite database files (含密码哈希/审计日志等，禁止提交)
*.db
*.db-shm
*.db-wal
# Production secrets / host kubeconfig (never commit)
.env
.env.*
!.env.example
configs/config.prod.yaml
/readonly.kubeconfig
*.kubeconfig
# Offline IP geo DB (downloaded at runtime; ~11MB)
/data/*.xdb
/data/*.xdb.tmp

